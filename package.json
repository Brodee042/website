{
  "name": "repos",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "Brody Arbon",
  "license": "ISC",
  "dependencies": {
    "cities": "^2.0.0",
    "express": "^4.19.2"
  },
  "devDependencies": {},
  "description": "This JSON and Javascript integration allows us to take the user responses and returns a page which tells them back to the user"
}
